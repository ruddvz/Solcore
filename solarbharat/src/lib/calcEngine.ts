/**
 * Plan0 §3 — financial engine entrypoint (canonical implementation: `finance.ts`).
 * Kept as a separate module so the spec path matches tooling and docs.
 */
export { calculateFinancials, landToAcres, acresToMwAc } from './finance'
