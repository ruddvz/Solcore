/**
 * Plan0 §9 — calculator store hook (Zustand).
 * Canonical implementation lives in `store/calculatorStore.ts`.
 */
export { useCalculatorStore as useStore } from '@/store/calculatorStore'
export type { CalculatorState as CalcState } from '@/store/calculatorStore'
